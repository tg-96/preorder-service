spring:
  data:
    redis:
      host: localhost
      port: 78
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

jwt:
  secret: ${SECRET_KEY}
  token-validity-in-seconds: 86400